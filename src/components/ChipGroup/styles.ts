import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	chipContainer: {
		flexDirection: "row",
		marginLeft: -15,
		marginRight: -15,
		paddingLeft: 10,
	},
});

export default styles;
